<h3>配送先編集</h3>
<%= form_with model: @address do |f| %>
<%= f.label :"郵便番号" %>
<%= f.text_field :postal_code %>

<%= f.label :"住所" %>
<%= f.text_field :address %>

<%= f.label :"宛名" %>
<%= f.text_field :name %>

<%= f.submit "変更を保存する" %>

<% end %>