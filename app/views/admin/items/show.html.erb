<h2>商品詳細</h2>

<% if @item.image.attached? %>
    <%= image_tag @item.image, size: "200x200" %>
  <% else %>
    <%= image_tag 'no_image', size: "200x200" %>
  <% end %>

<h4>商品名</h4>
<%= @item.name %>

<h4>商品説明</h4>
<%= @item.introduction %>

<h4>ジャンル</h4>
<%= @item.genre.name %>

<h4>
    税込価格<br>
    (税抜価格）
</h4>
<%= @item.price %>円 (<%= (@item.price / 1.1).round %>円)

<h4>販売ステータス</h4>
<%= @item.is_active %>

<%= link_to "編集する", edit_admin_item_path(@item.id), class: "btn btn-success" %>
